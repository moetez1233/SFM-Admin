import React, {
    useContext
} from 'react';
import {
    multistepContext
} from '../StepContext';
import {
    TextField,
    Button
} from '@material-ui/core';
import './style.css';

export default function InformationUser() {

    return ( <
        >

        <
        h1 > hello < /h1> < / >
    )
}